# 업데이트 로그

## 2026-05-29 온보딩 화면 헤엄 마이크로 인터랙션

- **새로운 UI 효과**: 인트로 화면(`scr-intro`)에 작은 붕어 이미지가 왼쪽↔️ 오른쪽으로 부드럽게 헤엄합니다.
- **구현 세부**:
  - `style.css`에 `@keyframes swim-left-to-right`와 `@keyframes swim-right-to-left` 추가.
  - `script.js`에 `initSwimmingBungeos()` 함수와 DOMContentLoaded 훅을 연결해 2.5초 간격으로 최대 4마리까지 스폰.
  - 랜덤 크기(20‑32px), 위치(15‑80% top), 속도(4.5‑7.5s)와 방향을 적용해 자연스러운 움직임 구현.
- **시각적 효과**: 메인 붕어 패밀리 이미지 뒤쪽에서 투명도 0.55로 등장해 살짝 페이드‑인한 뒤 사라집니다.

## 2026-05-29 장바구니 16가지 붕어빵 조합 그래픽

- **복합 이미지**: 빵 종류와 속재료 조합을 하나의 카드에 두 이미지(도우 + 재료 배지)로 표시.
- **구현**:
  - `script.js`의 `renderCart()`에 `graphicHtml`을 동적으로 만들어 도우 이미지와 속재료 배지를 겹쳐 렌더링.
  - `style.css`는 배지와 카드 레이아웃을 위한 미세 스타일을 이미 포함하고 있습니다.
- **결과**: 장바구니에 4×4 = 16가지 조합이 각각 고유한 비주얼로 표시됩니다.

## 기타 개선 사항

- 인트로 화면 레이어링을 위한 `.intro-graphic`에 `position: relative`, `overflow: hidden` 등 레이아웃 보강.
- GitHub에 모든 변경 사항을 커밋 및 푸시했습니다.

---

> 이 파일은 최신 UI/UX 업데이트 내역을 정리한 문서이며, 프로젝트 루트에 위치합니다. 필요에 따라 `README.md`에도 요약을 추가할 수 있습니다.
